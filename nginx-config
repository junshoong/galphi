server {
    listen 80;
    server_name;
    charset utf-8;

    location / {
        include proxy_params;
        proxy_pass http://unix:/tmp/galphi.sock;
    }

    location /static/ {
        root /var/galphi;
    }

    location /media/ {
        root /var/galphi;
    }
}
